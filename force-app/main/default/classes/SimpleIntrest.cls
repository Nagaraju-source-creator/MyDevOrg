public class SimpleIntrest  {
    public Decimal Smpi{set;get;}
    public decimal calculateIntrest(Decimal Principal , decimal Intrestrate,decimal Timeperiod ){
     smpi = (Principal*(Timeperiod/12)*Intrestrate)/100;
       return smpi;   
         }
}